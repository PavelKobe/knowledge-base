# InterviewLab — журнал учебных сессий

Журнал хранит историю курса. Старые записи не переписываются; новая сессия добавляется сверху.

## 2026-08-19 — Lifespan работает, добавлен фундамент TOML

### Подтверждено

- `httpx.AsyncClient` создаётся до `yield` и закрывается через `await aclose()` после `yield`;
- pytest проходит: `1 passed`;
- mypy проходит без ошибок;
- отдельный преподавательский тест подтвердил startup, доступность клиента во время запроса и закрытие при shutdown;
- ответы самозащиты M00.8.1 приняты.

### Требует завершения

- записанное доказательство `ruff — success` не совпало с фактической проверкой;
- в `lifecycle.py` и `main.py` остались две ошибки форматирования импортов `I001`;
- M00.8.1 остаётся в `rework` до зелёного Ruff.

### Добавлено в программу

- создан отдельный фундаментальный урок [[M00.8.1a — TOML и pyproject.toml с нуля]];
- разобраны типы TOML, таблицы, массивы таблиц, `pyproject.toml`, runtime/dev зависимости, extras, ограничения версий, классы ошибок и безопасная методика изменения;
- добавлены проверка понимания и небольшая практика `toml_lab.toml`.

### Следующий шаг

Изучить и заполнить [[M00.8.1a — TOML и pyproject.toml с нуля]], затем завершить Ruff-проверку M00.8.1.

## 2026-08-19 — M00.8 разбита на задачи

### Выполнено

- итоговая практика разделена на пять последовательных карточек;
- добавлены отдельные этапы lifespan, dependency, async-service, HTTP-границы и тестов;
- в backend создан отключённый каркас `lifecycle`, `dependencies`, `services/diagnostics` и `api/system`;
- каркас синтаксически проверен и пока не подключён к `main.py`;
- установлено, что Python 3.12 доступен, а `.venv` проекта ещё не создан.

### Следующий шаг

Выполнить только [[M00.8.1 — Окружение и lifespan]].

## 2026-08-18 — M00.7 завершён

### Подтверждено

- lifespan выполняет startup и shutdown один раз на экземпляр приложения;
- application-scoped ресурс переиспользуется запросами;
- lifespan владеет ресурсом и выполняет cleanup;
- `AsyncEngine` относится к application scope, `AsyncSession` — к request scope;
- каждый Uvicorn worker process создаёт собственные ресурсы.

### Следующий шаг

Самостоятельно выполнить [[M00.8 — Итоговая практика asyncio FastAPI]] в каркасе InterviewLab.

## 2026-08-17 — M00.6 завершён

### Подтверждено

- coroutine получает `CancelledError`;
- timeout-контекст преобразует свою отмену в `TimeoutError` для внешнего кода;
- `finally` выполняет cleanup при разматывании стека;
- незакрытые DB-соединения приводят к утечке ресурсов и исчерпанию pool.

### Следующий шаг

Пройти [[M00.7 — Lifespan FastAPI и владение ресурсами]].

## 2026-08-17 — Разбор эксперимента M00.6

### Наблюдение

Эксперимент выполнен, но прогноз и объяснение не различали внутренний `CancelledError` и внешний `TimeoutError`; продолжение `main` после обработанного исключения также было предсказано неверно.

### Следующий шаг

Изучить точечный разбор stack unwinding и заполнить три исправленных ответа в [[M00.6 — Timeout cancellation и cleanup]].

## 2026-08-17 — M00.5 завершён

### Подтверждено

- `time.sleep` блокирует event-loop thread;
- `asyncio.sleep` приостанавливает только Task;
- `async def` не изменяет модель sync-функции;
- для HTTP внутри async endpoint нужен async-клиент или контролируемая адаптация через thread pool.

### Следующий шаг

Пройти [[M00.6 — Timeout cancellation и cleanup]].

## 2026-08-17 — M00.4 завершён

### Подтверждено

- два последовательных `await` запускают операции одну за другой;
- `TaskGroup` позволяет перекрыть независимые I/O-ожидания;
- зависимые операции должны сохранять последовательный порядок.

### Следующий шаг

Пройти [[M00.5 — Blocking IO и живой event loop]].

## 2026-08-17 — M00.3 завершён

### Подтверждено

- coroutine object без `await` или планирования не исполняется;
- Task регистрирует coroutine в event loop;
- `await asyncio.sleep(0)` является точкой добровольной передачи управления;
- Task отличается от worker thread.

### Следующий шаг

Пройти [[M00.4 — Последовательный и конкурентный await]].

## 2026-08-17 — Теория M00.3 пройдена

### Подтверждено

- распознаны process, worker thread, thread pool и request queue;
- есть общее понимание роли Task и event loop.

### Уточнено

- generic thread и worker thread — не полные синонимы;
- вызов async-функции создаёт coroutine object;
- переключение Task происходит на незавершённом `await`, а не только при обращении к БД.

### Следующий шаг

Запустить пример раздела 7 M00.3, вставить фактический вывод и определить первую точку передачи управления event loop.

## 2026-08-13 — Добавлен простой словарь worker threads

В M00.3 термины process, thread, worker, worker thread, thread pool и request queue объяснены сначала через аналогию с офисом, а затем переведены обратно в техническую модель Waitress/Flask.

## 2026-08-13 — Уточнена роль asyncio в HTTP

### Добавлено в M00.3

- HTTP request отделён от модели выполнения Python-клиента и сервера;
- показана обработка одного запроса через Flask/Waitress и FastAPI/Uvicorn;
- объяснено, почему asyncio не требуется всегда;
- добавлен выбор между threads, async I/O, process и RabbitMQ;
- зафиксировано, что `asyncio.run()` не вызывают внутри FastAPI endpoint: event loop уже управляется ASGI-server.

## 2026-08-13 — Flask-runtime фундамент подтверждён

### Подтверждено

- пятый запрос ждёт при занятости всех worker threads;
- threads одного process делят память;
- processes имеют отдельную память, CPython и GIL;
- трассировка Flask-запроса пройдена.

### Следующий шаг

Перейти к [[M00.3 — Микроурок 1 Coroutine Task Event Loop]] и связать worker threads с async Task/event loop.

## 2026-08-13 — Разбор трассировки Flask

### Подтверждено

- понятна роль Waitress перед Flask;
- понятна передача запроса worker thread;
- понятны Flask request context и синхронное удержание thread.

### Требует точечного исправления

- process существует до прихода запроса;
- threads одного process делят heap и глобальные объекты;
- processes имеют раздельную память;
- пятый запрос ждёт при занятости всех threads;
- назначение WSGI `environ` и `start_response`.

### Следующий шаг

Заполнить три исправленных ответа в [[M00.0.1 — Трассировка Flask запроса]].

## 2026-08-13 — M00.0 изучен

### Выполнено

- ученик прочитал фундаментальный урок о CPython, process, threads, GIL, WSGI, Waitress и Flask;
- чтение зафиксировано отдельно от подтверждённого освоения;
- создано упражнение на трассировку одного реального Flask-запроса без балльного теста.

### Следующий шаг

Заполнить [[M00.0.1 — Трассировка Flask запроса]], затем вернуться к coroutine, Task и event loop.

## 2026-08-10 — Добавлен фундамент Python web runtime

### Основание

Ученик предоставил `app.py`, `app_factory.py` и `wsgi.py` действующего Flask-приложения и запросил фундаментальное объяснение процессов, потоков, workers и синхронного web runtime.

### Выполнено

- разобран фактический production entry point;
- установлено, что показанный `wsgi.py` импортирует готовый `app` из `app.py`;
- зафиксировано, что `app_factory.py` в показанном production-пути не используется;
- подготовлен материал о CPython, bytecode, frames, import, processes, threads и GIL;
- разобран путь запроса Waitress → WSGI → Flask context → view → response;
- добавлено сравнение Flask/WSGI и FastAPI/ASGI.

### Следующий шаг

Изучать [[M00.0 — Как Python исполняет Flask приложение]] по одной части, затем вернуться к M00.3.

## 2026-08-10 — Корректировка методики M00

### Наблюдение

Ученик правильно выбирает механизмы для готовых сценариев, но большая теоретическая глава не сформировала достаточно ясную модель исполнения coroutine, Task и event loop.

### Решение

- не выставлять итоговый балл за M00.2;
- не переходить сразу к большой лабораторной работе;
- разбить тему на микроуроки;
- в каждом микроуроке использовать одну модель, короткую трассировку и наблюдение реального вывода программы;
- проверять перенос понимания только после наблюдения.

### Следующий шаг

Пройти [[M00.3 — Микроурок 1 Coroutine Task Event Loop]].

## 2026-08-10 — Теория M00 изучена

### Выполнено

- ученик подтвердил изучение теории asyncio и lifecycle FastAPI;
- M00 переведён с этапа `theory` на этап `test`;
- создана короткая проверка понимания из восьми вопросов.

### Следующий шаг

Заполнить [[M00.2 — Проверка теории]], затем перейти к лабораторной работе.

## 2026-08-10 — Запуск курса

### Выполнено

- сформулирована цель подготовки к Middle Python Backend;
- проведена диагностика Python, FastAPI, PostgreSQL, Redis, RabbitMQ, frontend state и agentic engineering;
- определён уровень Junior+/начинающий Middle;
- выбрана концепция InterviewLab;
- создан каркас проекта и учебная дорожная карта;
- создан dashboard и матрица навыков;
- подготовлена теория по asyncio и lifespan FastAPI.

### Выявленные пробелы

- coroutine, Task и event loop;
- конкурентность и параллелизм;
- транзакции и конкурентные изменения;
- N+1;
- RabbitMQ delivery semantics;
- идемпотентность;
- разделение server/client state.

### Принятое методологическое решение

Порядок обучения: подробная теория → примеры → конспект → короткий тест → небольшая практика → применение в проекте → failure lab → review.

### Следующий шаг

Ученик изучает [[M00.1 — Теория asyncio и lifecycle FastAPI]].
